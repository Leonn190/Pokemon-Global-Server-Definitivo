// Menu principal: retangulo da logo em pixels (x, y, w, h) e forca do efeito.
uniform vec4 u_menu_logo_rect;
uniform float u_menu_logo_power;

// Texto cinemático no HUD: retangulo em pixels (x, y, w, h), forca e modo visual.
uniform vec4 u_texto_cinematico_rect;
uniform float u_texto_cinematico_power;
uniform float u_texto_cinematico_modo;
